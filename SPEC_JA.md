# SLIME CORE lunch 仕様書

## 1. 目的

SLIME CORE lunch は、Ollama 経由でローカルLLMと会話できる軽量版 Slimecore である。通常会話、音声読み上げ、モデル切り替え、会話ログ保存を中心に、日常的に軽く起動して使えることを目的とする。

「lunch」は手軽さ（お昼ごはんのように軽く使える）と起動（launch）の掛詞として扱う。

## 2. 対象環境

- macOS
- Python 3
- Streamlit
- Ollama
- macOS `say` コマンド
- NDLOCR-Lite（PDF処理時）
- Poppler `pdftoppm` と `pdfinfo`（PDF処理時）

## 3. 構成ファイル

- `app.py`: Streamlit アプリ本体
- `launch.command`: Finder ダブルクリック起動用スクリプト
- `requirements.txt`: Python 依存パッケージ
- `README_JA.md`: 利用者向け手順
- `SPEC_JA.md`: 本仕様書
- `assets/slimecore_flat.jpeg`: キャラクター画像
- `data/chats/`: 会話ログ保存先
- `last_launch.log`: 直近の起動ログ
- `app_config.json`: 古文処理の外部依存と閾値設定
- `modules/`: PDF、OCR、フィルタ、現代語訳、出力の処理モジュール
- `outputs/`: 古文処理セッションの成果物
- `tests/`: 古文処理と画面の自動テスト

## 4. 機能範囲

含める機能:

- Ollama `/api/chat` を使った会話生成
- Ollama `/api/tags` を使ったモデル一覧取得
- モデル選択
- temperature 調整
- 最大応答長調整
- macOS 音声読み上げ
- 音声自動読み上げ ON/OFF
- 音声種類の選択
- 読み上げ速度の調整
- 最後の返答の再読み上げ
- Enter 二度押し送信
- Enter 猶予時間の調整
- Enter 送信の ON/OFF
- 日本語 / English UI切り替え
- 選択言語に応じたLLM応答言語切り替え
- 最新回答表示
- 会話ログ保存
- 会話履歴クリア
- 起動時の空きポート自動選択
- 起動ログ保存
- PDF / OCR JSON の取り込み
- PDF全ページの画像化とページ単位OCR
- confidence と日本語文字比率による低信頼度判定
- 不明瞭行を `(原文不明瞭)` として残す現代語訳
- 訳文、OCR結果、フィルタ結果、manifestのセッション保存

含めない機能:

- 画像解析
- IRISCORE 連携
- Web 公開や外部サーバー運用

## 5. UI 仕様

画面はサイドバー＋メインの2カラム構成とする。

サイドバー:

- 日本語 / English 言語切り替え
- `SLIME CORE` タイトル
- キャラクター画像
- Ollama モデル選択
- temperature スライダー
- max response スライダー
- Enter 二度押し送信 ON/OFF
- Enter 猶予秒スライダー（1.0〜3.0秒）
- 自動読み上げトグル
- voice 選択
- speech rate スライダー
- 会話クリアボタン
- Ollama / 音声 / CPU / MEM 状態表示
- 現在のモデルと音声設定

メインは「会話」と「古文処理」のタブで分ける。

会話タブ:

1. コメント欄
2. コメントの真下に最新回答
3. その下に会話ログ

古文処理タブ:

- PDFまたはNDLOCR形式のJSONアップロード
- NDLOCR-Liteの導入状態と処理設定の表示
- バックグラウンド処理の進捗表示
- 現代語訳の表示と `translation.txt` / `manifest.json` のダウンロード

文字見切れ対策:

- 上部余白を確保する
- タイトル行高を十分に取る
- 日本語大見出しがブラウザ上部で切れないよう `main-title` の line-height と padding を広めに取る
- 会話領域をメイン全幅で使う
- 長文日本語が読みやすい横幅を確保する

## 6. 入力仕様

コメント欄:

- 入力欄はメイン上部に常設する
- 入力欄は `text_area` を使用する
- `Shift+Enter` で改行する
- 送信後は入力欄を空にする
- 応答待ち中は入力欄を無効化する

Enter 二度押し送信:

- Enter 1回目: 送信待機状態へ入る
- Enter 2回目: AIへ送信する
- 既定猶予時間: 1.8秒
- 猶予時間はサイドバーで 1.0〜3.0秒に調整できる
- 日本語変換中の Enter は送信しない
- 空文字は送信しない
- Enter 送信はサイドバーで OFF にできる
- 送信ボタン押下でも送信できる

送信待機フィードバック:

- Enter 1回目で送信ボタンの表示を変える
- 待機中のボタンラベルは日本語UIでは「もう一度 Enter で送信」、English UIでは「Press Enter again to send」
- 猶予時間の残りをボタン色のフェードで示す
- 猶予時間を超えるとボタン表示を元に戻す

## 7. 言語切り替え仕様

- サイドバー上部に `Language` を表示する
- 選択肢は `日本語` と `English`
- UIラベル、ヘルプ文、エラー文、最新回答プレースホルダーを選択言語へ切り替える
- Ollamaへ送るシステムプロンプトも選択言語へ切り替える
- English選択時は英語で自然かつ簡潔に返答する

## 8. 回答表示仕様

最新回答:

- コメント欄の真下に表示する
- 表示対象は直近の assistant メッセージ
- 会話前は「SLIME CORE の返答はここに表示されます。」を表示する
- 応答待ち中は「考えています...」を表示する
- 最新回答は会話ログとは別枠で表示する

応答待ち:

- Ollama へリクエスト送信後、最新回答エリアに待機表示を出す
- 応答待ち中はコメント欄と送信ボタンを無効化する
- 応答完了後にコメント欄と送信ボタンを有効化する

中断:

- 現行実装では未対応
- 将来、Ollama のストリーミング応答とバックグラウンド実行へ切り替える場合に実装する

## 9. 会話ログ仕様

画面表示:

- user / assistant のメッセージを時系列で表示する
- 新しい会話は下方向へ追加する
- UI表示は user + assistant 合計40メッセージまで
- 40件を超える場合、古いメッセージからUI表示を省略する
- UI上に「（古い会話は省略されています）」と表示する

Ollama 送信:

- システムプロンプト＋直近40メッセージ＋最新user入力を送る

JSON保存:

- JSON側には古い会話も残す
- JSON側の上限は設けない

## 10. 会話ログファイル仕様

保存先:

```text
data/chats/slimecore_chat_YYYYMMDD.json
```

ファイル日付:

- セッション開始時の日付で作成する
- 日をまたいでも同一セッション中はファイルを切り替えない
- 会話クリアまたはアプリ再起動でセッションを区切る

保存内容:

- session_started_at
- updated_at
- model
- messages
- role
- content
- time

## 11. Ollama 仕様

接続先:

- `http://127.0.0.1:11434`
- モデル一覧: `/api/tags`
- 会話生成: `/api/chat`

既定モデル:

- `gemma4:26b`

モデル優先順:

1. `gemma4:26b`
2. `gemma4:31b`
3. `gemma3:27b`
4. `gemma4:e4b`
5. `gemma3:12b`
6. `gemma3:latest`

Ollama 未接続表示:

- Streamlit UI 上のサイドバーに警告を表示する
- Ollama コマンド未インストール時はインストール案内を表示する
- Ollama インストール済みだがサーバー未起動時は接続不可を表示する

## 12. 音声仕様

読み上げ:

- macOS の `say` コマンドを使用する
- assistant の新規返答を自動読み上げする
- 自動読み上げは ON/OFF 可能
- 読み上げ対象は返答先頭900文字まで

音声選択:

- `say -v ?` で利用可能な音声を取得する
- 日本語音声を優先して表示する
- 英語音声も一部表示する
- 取得できない場合は `System Default` を使う

900文字超過通知:

- 返答が900文字を超えた場合、最新回答エリア下部に注記を表示する
- 注記文言は「（読み上げは先頭 900 文字までです）」
- 注記は次の返答まで残す

再読み上げ:

- `最後の返答をもう一度読む` ボタンで直近の assistant 返答を再生する

## 13. 起動仕様

起動方法:

```bash
cd ~/Desktop/SLIMEcore_lunch
./launch.command
```

ポート選択:

- 既定ポートは `8502`
- `8502` が使用中の場合は `8503`, `8504`... と順に試す
- 最大 `BASE_PORT + 20` まで探索する
- 空きポートが見つからない場合は起動失敗とする

ブラウザ起動:

- Streamlit サーバーを起動する
- `/_stcore/health` が `ok` を返すまで待つ
- 応答確認後にブラウザを開く

起動ログ:

- `last_launch.log` に保存する
- 起動失敗時はターミナルをすぐ閉じず、ログの場所を表示する

## 14. 古文処理仕様

- PDFは全ページをPNGへ変換してからページ単位でOCRする
- OCR結果は `contents[].{id,boundingBox,isVertical,text,isTextline,confidence}` と `imginfo` の共通スキーマへ正規化する
- `confidence < 0.5` または日本語文字比率 `< 0.5` の行は翻訳せず、`(原文不明瞭)` を訳文へ挿入する
- OCRがconfidenceを返さない場合は安全側で不明瞭として扱い、manifestへ警告を残す
- 長文は句点を優先してチャンク化し、Ollamaには現代語訳だけを生成させる
- セッションごとに `pages/`、`ocr/`、`filtered/`、`translation/translation.txt`、`manifest.json` を保存する
- 古文処理はバックグラウンドで実行し、会話タブの操作を妨げない
- NDLOCR-Lite未導入時はPDF処理を開始せず、利用者が次の行動を判断できる案内を表示する

## 15. 受け入れ条件

起動:

- `launch.command` のダブルクリックで起動できる
- 8502 が使用中でも別ポートで起動できる
- サーバー応答後にブラウザが開く
- 起動失敗時に `last_launch.log` が残る

UI:

- 上部タイトルが見切れない
- サイドバー＋メインの2カラムになっている
- 上部の大見出しが見切れない
- Languageで日本語 / Englishを切り替えられる
- コメント欄がメイン上部にある
- 最新回答がコメント欄の真下に表示される
- 会話ログが最新回答の下に続く

入力:

- Enter 1回で送信されない
- Enter 2回で送信される
- Enter 1回目で送信ボタンの表示が変わる
- Enter 2回目で実際に送信ボタンがクリックされる
- 猶予時間経過で送信ボタン表示が戻る
- 日本語変換中の Enter で送信されない
- Shift+Enter で改行できる
- 送信ボタンでも送信できる

会話:

- Ollama の選択モデルへ問い合わせる
- 応答待ち中に「考えています...」が表示される
- assistant 返答が最新回答に表示される
- user / assistant のログが会話ログに残る
- 40件を超えた古いログはUI上で省略される
- JSONログには全件保存される

音声:

- 自動読み上げ ON のとき assistant 返答を読み上げる
- 自動読み上げ OFF のとき読み上げない
- 最後の返答を再読み上げできる
- speech rate が読み上げ速度に反映される
- 900文字超過時に注記が出る

古文処理:

- PDFを選択したとき、NDLOCR-Liteが利用可能なら全ページの処理を開始できる
- OCR JSONはNDLOCR-Lite未導入でもフィルタと訳文処理を実行できる
- ID 15、22、23、31のフィクスチャ判定が仕様どおりになる
- 処理完了後に訳文、manifest、OCR結果、フィルタ結果が同一セッションへ保存される
- 日本語 / Englishの切り替え後も会話・古文処理タブが表示される

## 16. 今後の拡張候補

- 応答中断ボタン
- Ollama ストリーミング表示
- 会話ログの読み込み機能
- キャラクター表情差分
- 軽量モデル優先モード
- 画像解析モードの再追加
